/**
 * Public referral defaults and stable routes (keep in sync with fin_backend `publicReferral.js`).
 */
export const PUBLIC_LOAN_REFERRAL_FALLBACK_PARTNER_CODE = "PT-D4CTD8B2";

/**
 * Partner self-registration path (leading slash for React Router).
 * Must match backend `PARTNER_REGISTRATION_PATH_SEGMENT`.
 */
export const PARTNER_REGISTRATION_ROUTE = "/PartnerRegistrationForm";
